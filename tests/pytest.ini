[pytest]
log_cli = false
testpaths = tests
addopts = -rsxX -l --tb=short --strict-markers
markers =
    only_this: mark new test debug
    slow: mark test as slow.
    moke: mark smoke test.
    regress: mark for regression test suite.